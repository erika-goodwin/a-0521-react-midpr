// --- Directions
// Check to see if two provided strings are anagrams of eachother.
// One string is an anagram of another if it uses the same characters
// in the same quantity. Only consider characters, not spaces
// or punctuation.  Consider capital letters to be the same as lower case
// --- Examples
//   anagrams(‘rail safety’, ‘fairy tales’) --> True
//   anagrams(‘RAIL! SAFETY!’, ‘fairy tales’) --> True
//   anagrams(‘Hi there’, ‘Bye there’) --> False

function anagrams(stringA, stringB) {
  //   //solution1
  //   const aChatMap = buildChartMap(stringA); //{a:1, b:2, c:3}
  //   const bCharMap = buildChartMap(stringB); //{c:1, e:5, x:1}
  // //   const obj = { a: 1, b: 2, c: 3 };
  // //   console.log(Object.keys(obj)); //Object: class
  // if(Object.keys(aChatMap).length !== Object.keys(bCharMap.length){
  //     return false
  // }
  // for(let char in aChatMap){
  //     if(aChartMap[char] !== buildChartMap[char]){
  //         return false
  //     }
  // }
  // console.log(anagrams("rail safety", "fairy tales"));
  // function buildChartMap(str) {
  //   const charMap = {};
  //   // /   / <--- to start a regex
  //   // /g <--- g flag for global
  //   // [ˆ\w] <--- character that is no a character, replace with an empty st
  //   for (let char of str.replace(/[ˆ\w]/g, "").toLowerCase()) {
  //   }
  //solution2
}
